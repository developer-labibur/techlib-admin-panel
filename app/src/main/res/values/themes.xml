<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TechLibAdminPanel" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Transparent status bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Keep default navigation bar -->
        <!-- <item name="android:navigationBarColor">?android:colorBackground</item> -->
        <item name="android:navigationBarColor">@color/mainContainer_HeadingText</item>
        <!-- Optional: Light status bar icons (dark text/icons on light background) -->
        <item name="android:windowLightStatusBar">true</item>

        <!-- Transparent status bar -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue__LavenderBlue</item>
        <item name="colorPrimaryVariant">@color/blue__LavenderBlue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green__VividCyan</item>
        <item name="colorSecondaryVariant">@color/green__DarkCyan</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.TechLibAdminPanel" parent="Base.Theme.TechLibAdminPanel" />

</resources>